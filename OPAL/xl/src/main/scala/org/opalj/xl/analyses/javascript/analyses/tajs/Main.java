package org.opalj.xl.analyses.javascript.analyses.tajs;
/*
import dk.brics.tajs.analysis.Analysis;
import dk.brics.tajs.analysis.InitialStateBuilder;
import dk.brics.tajs.options.Options;
import dk.brics.tajs.util.AnalysisException;

import static dk.brics.tajs.Main.run;
*/
/**
 * Main class for the TAJS program analysis.
 *//*
public class Main {

    public  void start() {
        try {
            dk.brics.tajs.Main.initLogging();
            Analysis a = dk.brics.tajs.Main.init(args, null);
            if (a == null)
                System.exit(-1);
            run(a);

            //a.getSolver()

             InitialStateBuilder isb =  a.getInitialStateBuilder();

             a.getBlendedAnalysis().

        } catch (AnalysisException e) {
            if (Options.get().isDebugOrTestEnabled()) {
                throw e;
            }
        }
    }

}

*/